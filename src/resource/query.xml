<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

    <!-- 로그인 요청 -->

    <!-- 발주 요청 -->
    <entry key="getProdcutList">
        {call get_productlist_proc(?)}
    </entry>

    <entry key="createOrder">
        {call insert_order_proc(?, ?)}
    </entry>

    <entry key="insertOrderList">
        {call in_orderlist_proc(?, ?, ?)}
    </entry>

    <!-- 발주 조회 -->
    <entry key="getOrderList">
        {CALL get_recent_orders_paging(?, ?, ?)}
    </entry>

    <!-- 발주 상세 조회 -->
    <entry key="getOrderDetail">
        {CALL get_order_details(?, ?)}
    </entry>

    <!-- 결제 조회 -->
    <entry key="getTransacList">
        {CALL get_transactions_paging(?, ?, ?)}
    </entry>

    <!-- 결제 상세 조회 -->
    <entry key="getTransacDetail">
        {CALL get_transaction_details(?, ?)}
    </entry>

    <!-- 통계 확인 -->
    <entry key="getProductListS">
        {call get_productLists_proc(?)}
    </entry>

    <!-- 통계 데이터 조회 -->
    <entry key="getStatistics">
        {CALL get_weekly_statistics(?, ?, ?, ?, ?)}
    </entry>

</properties>